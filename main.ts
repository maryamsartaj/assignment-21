interface item {
    name: string
    price: number
}
//tow objects

const vegetable: item ={
    name:`tomato`
    price: 100

}

const froit: item ={
    name: `apple`
    price: 200
}

console.log(`vegetable name: ${vegetable.name}, vegetable price: ${vegetable.price}`)
console.log(`froit name: ${froit.name},froit price: ${froit.price}`)